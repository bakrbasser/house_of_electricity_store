import 'package:flutter/material.dart';

class ShareProduct extends StatelessWidget {
  const ShareProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(onPressed: () {}, icon: Icon(Icons.ios_share));
  }
}
